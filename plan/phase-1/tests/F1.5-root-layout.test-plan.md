# F1.5 — Root Layout & Font Loading: Test Cases

## Test File: `src/app/[locale]/__tests__/layout.test.tsx`

---

### Unit Tests

#### TC-1.5.1: Layout renders children
```
Test: Render layout with <p>Test Content</p> as children
Assert: 'Test Content' appears in rendered output
```

#### TC-1.5.2: HTML lang attribute set to 'en'
```
Test: Render layout with locale='en'
Assert: <html> element has lang="en"
```

#### TC-1.5.3: HTML lang attribute set to 'bn-BD'
```
Test: Render layout with locale='bn-BD'
Assert: <html> element has lang="bn-BD"
```

#### TC-1.5.4: HTML lang attribute set to 'sv'
```
Test: Render layout with locale='sv'
Assert: <html> element has lang="sv"
```

#### TC-1.5.5: HTML dir attribute is always 'ltr'
```
Test: Render layout with each locale
Assert: <html> element has dir="ltr" for all 3 locales
```

---

### Integration Tests — Font Loading

#### TC-1.5.6: Inter font applied to all locales
```
Test: Render layout with locale='en'
Assert: <body> has Inter font className
Test: Render layout with locale='sv'
Assert: <body> has Inter font className
Test: Render layout with locale='bn-BD'
Assert: <body> has Inter font className
```

#### TC-1.5.7: Noto Sans Bengali applied ONLY for bn-BD
```
Test: Render layout with locale='bn-BD'
Assert: <body> className includes Noto Sans Bengali class
Test: Render layout with locale='en'
Assert: <body> className does NOT include Noto Sans Bengali class
Test: Render layout with locale='sv'
Assert: <body> className does NOT include Noto Sans Bengali class
```

#### TC-1.5.8: Bengali locale has increased line-height
```
Test: Render layout with locale='bn-BD'
Assert: <body> has a class that sets line-height >= 1.7
       (e.g., 'leading-[1.75]' or equivalent Tailwind class)
Test: Render layout with locale='en'
Assert: <body> does NOT have the Bengali line-height class
```

---

### Integration Tests — Providers

#### TC-1.5.9: NextIntlClientProvider wraps content
```
Test: Render layout with locale='en' and children that call useTranslations('common')
Assert: useTranslations hook resolves without error
Assert: Returns translated string from en.json
```

#### TC-1.5.10: Layout loads correct message file per locale
```
Test: Render layout with locale='bn-BD'
      Children call useTranslations('nav')
Assert: Returns Bengali translations from bn-BD.json
```

---

### Integration Tests — Metadata

#### TC-1.5.11: Default metadata title template
```
Test: Check generateMetadata or metadata export for locale='en'
Assert: title.template is '%s | BestFinds' (or similar pattern)
Assert: title.default is a non-empty string
```

#### TC-1.5.12: Metadata description varies by locale
```
Test: Check metadata for locale='en' vs locale='bn-BD'
Assert: Descriptions are different (locale-specific)
```

---

### Integration Tests — Affiliate Disclosure

#### TC-1.5.13: Affiliate disclosure text present
```
Test: Render layout with locale='en'
Assert: Rendered HTML contains 'As an Amazon Associate, we earn from qualifying purchases'
       OR the equivalent next-intl translation key is rendered
```

#### TC-1.5.14: Affiliate disclosure present in all locales
```
Test: Render layout with locale='bn-BD'
Assert: Affiliate disclosure text is present (in Bengali or English)
Test: Render layout with locale='sv'
Assert: Affiliate disclosure text is present (in Swedish or English)
```

---

### Edge Case Tests

#### TC-1.5.15: Layout handles empty children
```
Test: Render layout with no children (or null)
Assert: Layout renders without crashing
Assert: HTML structure (html > body) is intact
```

#### TC-1.5.16: Font display swap configured
```
Test: Inspect Inter font configuration
Assert: display property is 'swap'
Test: Inspect Noto Sans Bengali font configuration
Assert: display property is 'swap'
Note: Prevents FOIT (Flash of Invisible Text)
```

#### TC-1.5.17: Layout with deeply nested children
```
Test: Render layout with 5 levels of nested divs as children
Assert: All content renders correctly
Assert: No provider context issues
```

---

### Failure Scenarios

#### TC-1.5.18: Invalid locale parameter
```
Test: Render layout with locale='fr' (not in supported list)
Assert: Either notFound() is called or falls back to 'en'
Note: Middleware should prevent this, but layout should be defensive
```

#### TC-1.5.19: Missing messages file for locale
```
Test: Mock messages import to throw for a locale
Assert: Layout handles error gracefully (error boundary or fallback)
Note: This scenario shouldn't occur in production but tests resilience
```

#### TC-1.5.20: NEXT_PUBLIC_SITE_URL not set
```
Test: Unset NEXT_PUBLIC_SITE_URL
      Check metadata.metadataBase
Assert: Is undefined or falls back to localhost
Assert: Layout still renders (non-fatal)
```

---

## Test Coverage Summary

| Area | Test Count | Types |
|---|---|---|
| HTML attributes (lang, dir) | 5 | Unit |
| Font loading | 3 | Integration |
| Line-height | 1 | Integration |
| Provider integration | 2 | Integration |
| Metadata | 2 | Integration |
| Affiliate disclosure | 2 | Integration |
| Edge cases | 3 | Edge case |
| Failure scenarios | 3 | Failure |
| **Total** | **21** | |
