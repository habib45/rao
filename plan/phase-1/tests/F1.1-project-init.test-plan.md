# F1.1 — Project Init: Test Cases

## Test File: `__tests__/config/project-init.test.ts`

### Unit Tests

#### TC-1.1.1: TypeScript compilation passes
```
Test: Run `tsc --noEmit` programmatically
Assert: Exit code 0, no errors in stdout
Type: Unit
Coverage: tsconfig.json validity
```

#### TC-1.1.2: Path alias @/* resolves
```
Test: Import from '@/types/domain' in a test file
Assert: Import resolves without errors
Type: Unit
Coverage: tsconfig paths configuration
```

#### TC-1.1.3: Environment variables documented
```
Test: Read .env.example, parse lines
Assert: Contains NEXT_PUBLIC_SUPABASE_URL
Assert: Contains NEXT_PUBLIC_SUPABASE_ANON_KEY
Assert: Contains NEXT_PUBLIC_SITE_URL
Assert: Does NOT contain real values (no '=' followed by non-empty value after the placeholder)
Type: Unit
Coverage: .env.example completeness
```

### Integration Tests

#### TC-1.1.4: Vitest configuration loads
```
Test: Import vitest config and validate shape
Assert: environment === 'jsdom'
Assert: resolve.alias includes '@' mapping
Type: Integration
Coverage: vitest.config.ts
```

#### TC-1.1.5: Tailwind CSS processes utility classes
```
Test: Render a component with Tailwind classes (e.g., 'bg-amber-500 text-white')
Assert: Classes are applied to the DOM element
Type: Integration
Coverage: Tailwind + PostCSS pipeline
```

### Edge Case Tests

#### TC-1.1.6: No secrets in source code
```
Test: Recursively grep src/ for patterns: 'service_role', 'AMAZON_SECRET', 'AMAZON_ACCESS_KEY', 'sk_live', 'eyJ' (JWT prefix)
Assert: Zero matches
Type: Security
Coverage: Secret leak prevention
```

### Failure Scenarios

#### TC-1.1.7: Missing dependency graceful error
```
Test: Attempt to import a non-existent package
Assert: Module not found error is clear and actionable
Type: Failure
Coverage: Dependency resolution
```
