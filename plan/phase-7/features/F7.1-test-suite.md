# F7.1 — Full Vitest Test Suite

## Test Coverage Targets

| Module | Target Coverage |
|---|---|
| `src/lib/amazon/` | 100% |
| `src/lib/cart/` | 100% |
| `src/lib/queries/` | 90%+ |
| `src/lib/i18n/` | 100% |
| `src/components/` | 80%+ |
| `src/app/[locale]/` pages | Key paths |
| `src/middleware.ts` | 100% |
| `src/app/admin/_lib/` | 100% |

## Test Categories

### Regression Tests
- Re-run all Phase 1–6 tests
- Ensure no regressions from admin panel additions

### Integration Tests
- Supabase query helpers (mocked supabase client)
- PA-API transformers with real-shaped fixture data
- Cart localStorage round-trips in jsdom

### E2E Smoke (manual / Playwright optional)
- Visit `/en` → page loads
- Add product to cart → count badge updates
- Visit `/en/cart` → item visible
- `/admin/login` → redirects unauthenticated users

## Acceptance Criteria

- [ ] All existing tests still pass (no regressions)
- [ ] New integration tests added for query helpers
- [ ] vitest coverage report generated
- [ ] No skipped tests (`it.skip` removed)
