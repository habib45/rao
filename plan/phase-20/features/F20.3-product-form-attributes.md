# F20.3 — Product Form: Attributes Editor + show_in_comparison

## Shared component
`src/app/admin/products/_components/AttributesEditor.tsx`
- Renders `attributes` JSONB as editable rows: [key input] → [value input] [× remove]
- "+ Add attribute" button appends empty row
- onChange emits `Record<string, string>`

## show_in_comparison toggle
- Toggle (existing Toggle UI primitive)
- Label: "Show in Comparison" / "Eligible to appear as comparison candidate on other product pages"

## Integration
Both `ProductEditForm.tsx` and `ProductCreateForm.tsx` — add to a new "Comparison" tab or append to General tab.
Decision: add to General tab (keeps it simple, avoids new tab).
