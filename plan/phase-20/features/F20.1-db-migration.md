# F20.1 — DB Migration

## Changes
1. Add `show_in_comparison BOOLEAN NOT NULL DEFAULT false` to `products`
2. Add GIN-friendly index on the column
3. Seed `admin_settings` key `comparison` with default keys

## Migration file
`supabase/migrations/00020_comparison.sql`

## Default comparison keys (seed)
Color, Weight, Power Type, Battery Life, Cleaning Path, Accessories Included, Warranty, Dimensions, Material, Connectivity
